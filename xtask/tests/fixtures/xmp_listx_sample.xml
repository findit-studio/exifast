<?xml version='1.0' encoding='UTF-8'?>
<!-- Generated by Image::ExifTool 13.59 -->
<!-- Trimmed capture of `exiftool -listx` for the XMP::tiff table (3 tags),
     used by the xtask listx-parser unit tests. The real `-listx` dumps every
     table (~18 MB) and ignores tag filters, so this is a hand-trimmed slice of
     the genuine 13.59 output: one tag with a <values> PrintConv map (with the
     real multi-<val>-per-key, lang-attributed shape incl. an &amp; entity),
     one plain integer tag (no map), and one plain string tag (with a per-tag
     g2 override). The tag header shape is verbatim: `type` carries the writable
     KIND while `writable` is the boolean true/false. -->
<taginfo>

<table name='XMP::tiff' g0='XMP' g1='XMP-tiff' g2='Image'>
 <desc lang='en'>XMP tiff</desc>
 <tag id='Compression' name='Compression' type='integer' writable='true'>
  <desc lang='en'>Compression</desc>
  <desc lang='de'>Komprimierungsschema</desc>
  <values>
   <key id='1'>
    <val lang='en'>Uncompressed</val>
    <val lang='de'>Nicht komprimiert</val>
   </key>
   <key id='6'>
    <val lang='en'>JPEG (old-style)</val>
    <val lang='de'>JPEG (alte Version)</val>
   </key>
   <key id='9'>
    <val lang='en'>JBIG B&amp;W or VC-5</val>
   </key>
  </values>
 </tag>
 <tag id='ImageWidth' name='ImageWidth' type='integer' writable='true'>
  <desc lang='en'>Image Width</desc>
  <desc lang='de'>Bildbreite</desc>
 </tag>
 <tag id='Make' name='Make' type='string' writable='true' g2='Camera'>
  <desc lang='en'>Make</desc>
  <desc lang='de'>Gerätehersteller</desc>
 </tag>
</table>

</taginfo>
